<?php
	$pronavStatus = Mage::getStoreConfig('pronav/pronavconfig/pronav_status');
	$posSelected = Mage::getStoreConfig('pronav/pronavstyle/pronav_position');
	
	if ($pronavStatus) {
		if ($posSelected == "content") {
			echo $this->getLayout()->getBlock('catalog.topnav.content')->setTemplate('pronav/navigation_top.phtml')->toHtml();
		}
	}
?>